#define minimum(a,b) a = ((a) < (b) ? (a) : (b))
#define maximum(a,b) a = ((a) > (b) ? (a) : (b))
#define FUNCTION(a,b)
#define foreach(v, j) for(int j = 0; j < v.size(); j++)
#define io(vec) cin >> vec
#define toStr(str) #str
#define INF 0
